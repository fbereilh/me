{
  "$schema": "https://raw.githubusercontent.com/jetpack-io/devbox/main/.schema/devbox.schema.json",
  "packages": [
    "bun@latest",
    "quarto@latest",
    "uv"
  ],
  "shell": {
    "init_hook": [
      "echo 'Welcome to your Svelte development environment!'",
      "echo 'Bun version:' && bun --version",
      "if [ ! -d \"node_modules\" ]; then echo 'Installing dependencies...' && bun install; fi",
      "if [ ! -d \".venv\" ]; then echo 'Creating Python virtual environment...' && uv venv; fi"
    ],
    "scripts": {
      "install": "bun install",
      "dev":     "bun run dev",
      "build":   "bun run build",
      "preview": "bun run preview",
      "render":  "cd nbs && quarto render"
    }
  }
}
